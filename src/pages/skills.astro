---
import LocalizedSkills from '../components/LocalizedSkills.astro';
import { profile } from '../data/en/profile';
import { capabilities, toolGroups, developingAreas } from '../data/en/skills';
---

<LocalizedSkills locale="en" profile={profile} capabilities={capabilities} toolGroups={toolGroups} developingAreas={developingAreas} />
