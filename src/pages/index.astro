---
import LocalizedHome from '../components/LocalizedHome.astro';
import { profile } from '../data/en/profile';
import { nowItems } from '../data/en/now';
import { projects } from '../data/en/projects';
import { work } from '../data/en/work';
import { academic } from '../data/en/academic';
import { skills } from '../data/en/skills';
---

<LocalizedHome locale="en" profile={profile} nowItems={nowItems} work={work} projects={projects} academic={academic} skills={skills} />
