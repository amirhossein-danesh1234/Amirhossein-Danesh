---
import LocalizedSkills from '../../components/LocalizedSkills.astro';
import { profile } from '../../data/fa/profile';
import { capabilities, toolGroups, developingAreas } from '../../data/fa/skills';
---

<LocalizedSkills locale="fa" profile={profile} capabilities={capabilities} toolGroups={toolGroups} developingAreas={developingAreas} />
