---
import LocalizedProjects from '../components/LocalizedProjects.astro';
import { profile } from '../data/en/profile';
import { projects } from '../data/en/projects';
---

<LocalizedProjects locale="en" profile={profile} projects={projects} />
